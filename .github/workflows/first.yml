name: second workflow 
on: 
    push:
        branches:
            main
jobs:
    run:
        runs-on: ubuntu-latest
        steps:
            - name: running
              run : this is step1
            
            - name: running1
              run : this is step2

            - name: run & 2
              run : running | running1
              
              